[build]
  command = "bundle exec jekyll build"
  publish = "_site"

[build.environment]
  JEKYLL_ENV = "production"

[dev]
  command = "bundle exec jekyll serve"
  targetPort = 4000
  port = 8888
  publish = "_site"
  autoLaunch = false
